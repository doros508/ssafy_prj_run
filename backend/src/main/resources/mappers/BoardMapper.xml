<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mrt.mvc.model.dao.BoardDao">

	<!-- 게시글 전체 조회 -->
	<select id="selectAll" resultType="Board" parameterType="SearchCondition">
		SELECT 
		    board_no, board_title, board_content, board_reg_date, board_view_cnt
		FROM board
        <!-- JOIN user ON board.user_no = user.user_no 유저 완성되면 주석 해제-->
	    <if test="key != null">
	    	WHERE ${key} LIKE concat('%',#{word},'%')
	    </if>
	    <if test="orderBy != null">
	    	ORDER BY ${orderBy} ${orderByDir}
	    </if>
	</select>
	
	<!-- 게시글 상세 조회 -->
	<select id="selectOne" resultType="Board" parameterType="int">
		SELECT board_no, board_title, board_content, board_reg_date, board_view_cnt
		FROM board
		WHERE board_no = #{no}
        <!-- JOIN user ON board.user_no = user.user_no 유저 완성되면 주석 해제-->
	</select>
	
	<!-- 조회수 증가 -->
	<update id="updateViewCnt" parameterType="int">
		UPDATE board
		SET board_view_cnt = board_view_cnt + 1
		WHERE board_no = #{no}
	</update>

	<!-- 게시글 등록 -->
	<insert id="insertBoard" parameterType="Board" keyProperty="boardNo" useGeneratedKeys="true">
	    INSERT INTO board (board_title, board_content, user_no)
	    VALUES (#{boardTitle}, #{boardContent}, #{userNo})
	</insert>
	
	<!-- 파일 등록 -->
	<insert id="insertBoardFile" parameterType="BoardFile">
		INSERT INTO files(file_no, file_name, file_path, file_system_name, board_no)
		VALUES (#{fileNo}, #{oriName}, #{path}, #{systemName}, #{boardNo});
	</insert>

	<!-- 게시글 내용 수정 -->
	<update id="updateBoard" parameterType="Board" >
		UPDATE board
		SET board_title = #{boardTitle}, board_content = #{boardContent}
		WHERE board_no = #{boardNo};
	</update>

	<!-- 게시글 파일 수정 -->
	<update id="updateFile">
		UPDATE files 
		SET file_name = #{fileName}, file_path = #{filePath}, file_system_name = #{fileSystemName}
	    WHERE board_no = #{boardNo} AND file_no = #{fileNo};
	</update>

	<!-- 게시글 삭제 -->
	<delete id="deleteBoard" parameterType="int">
		DELETE 
		FROM board
		WHERE board_no = #{no};
	</delete>
	
</mapper>